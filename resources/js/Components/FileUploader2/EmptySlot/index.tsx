export function EmptySlot() {

    return (
        <div className={"cursor-pointer max-h-64 rounded bg-teal-100 border-gray-400 text-teal border h-48 flex items-center justify-center"}>
            + video
        </div>
    );
}
